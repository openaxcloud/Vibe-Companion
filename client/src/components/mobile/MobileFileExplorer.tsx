export function MobileFileExplorer({ isOpen, onClose, onFileSelect, projectId, currentFileId }: {
  isOpen: boolean;
  onClose: () => void;
  onFileSelect: (file: any) => void;
  projectId?: number;
  currentFileId?: number;
}) {
  return null;
}

export default MobileFileExplorer;
