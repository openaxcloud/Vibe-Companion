export function MobileCreateModal({ isOpen, onClose, onCreate }: {
  isOpen: boolean;
  onClose: () => void;
  onCreate: (template: any) => void;
}) {
  return null;
}

export default MobileCreateModal;
