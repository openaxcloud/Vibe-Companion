Dev server script using esbuild serve API to serve index.html and proxy /api requests to the backend at http://localhost:4000. Enables HMR-like auto-reload via browser refresh.

Content generated as fallback. Please implement specific functionality as needed.
