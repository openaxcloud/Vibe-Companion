# Active Context – Current Focus & Next Steps

## Current Focus: Initial Setup
- Establish project structure for a TypeScript React frontend and Node backend.
- Configure TypeScript, linting, formatting, and shared types.
- Set up basic auth and Stripe integration scaffolding.

## Immediate Next Steps (Checklist)
- [ ] Initialize repository and monorepo (if used) with `client` and `server` folders.
- [ ] Configure TypeScript, ESLint, Prettier, and basic CI workflow.
- [ ] Set up Postgres (Docker) and connection via ORM (Prisma/TypeORM) with initial schema.
- [ ] Implement basic user model + JWT auth endpoints (signup/login/logout/current-user).
- [ ] Scaffold product catalog API (list/detail) and seed sample data.
- [ ] Create React app shell with routing, layout, and basic theming.
- [ ] Implement minimal product listing page consuming the API.
- [ ] Integrate Stripe on backend (test keys) and create a test payment intent endpoint.
- [ ] Add `.env` templates for client/server with the required variables.
- [ ] Document local dev commands and environment setup in a root README.
