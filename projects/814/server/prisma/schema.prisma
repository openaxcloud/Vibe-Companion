Prisma schema defining PostgreSQL datasource and models: User (id, email, passwordHash, role), Category, Product, ProductImage, Order, OrderItem, Cart, CartItem. Relations and indexes for common queries.

Content generated as fallback. Please implement specific functionality as needed.
