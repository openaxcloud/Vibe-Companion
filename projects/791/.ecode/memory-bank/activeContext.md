# Memory Bank — Active Context & Next Steps

- **Current Focus:** Initial setup and foundational decisions for the e-commerce marketplace.
- **Recent / Pending Decisions to Capture:**
  - Choose backend framework (e.g., Express vs Nest) and record rationale.
  - Decide API style (REST vs GraphQL) and versioning approach.
  - Decide on ORM (Prisma vs TypeORM) and DB (likely PostgreSQL).
  - Choose UI framework/styling (Tailwind vs component library).
  - Define initial data model (users, products, carts, orders, payments).
- **Immediate Next Steps (Checklist):**
  - [ ] Initialize monorepo or single repo with frontend and backend packages.
  - [ ] Scaffold React app with TypeScript and routing.
  - [ ] Scaffold Node/TS backend with healthcheck endpoint.
  - [ ] Configure database connection and migrations; create base schemas.
  - [ ] Integrate basic auth (sign up, login, protected routes).
  - [ ] Implement minimal product listing API and UI.
  - [ ] Set up Stripe in test mode (keys, basic payment intent flow).
  - [ ] Implement basic logging and .env management.
  - [ ] Start Memory Bank change-log for decisions, schema changes, and API contracts.
