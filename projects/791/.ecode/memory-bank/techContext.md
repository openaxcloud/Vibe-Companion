# Memory Bank — Tech Stack & Environment

- **Frontend:**
  - TypeScript + React (SPA with routing).
  - UI library and styling approach TBD (e.g., Tailwind, MUI, or custom CSS).
  - State management: React Query / SWR for server state; minimal local/global state.
- **Backend:**
  - TypeScript Node.js server framework (decision to be recorded when chosen).
  - ORM (e.g., Prisma or TypeORM) to manage database schema and queries.
  - REST or GraphQL API (must be recorded once decided, including versioning strategy).
- **Database & Infra:**
  - Relational DB (e.g., PostgreSQL) for marketplace entities.
  - Migrations maintained via ORM or dedicated tool.
  - Deployment target TBD (e.g., Vercel + managed DB, or container-based), to be tracked.
- **Payments & Auth:**
  - Stripe for payments, webhooks for confirmation and refunds.
  - AuthN/AuthZ stack TBD (e.g., JWT sessions, NextAuth, custom solution) and must be documented.
- **Key Dependencies to Record in Memory Bank:**
  - React version, router, state libraries.
  - Backend framework and ORM versions.
  - Stripe SDK version and webhook integration details.
- **Environment Variables (to track, not values):**
  - `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`.
  - `DATABASE_URL`.
  - App secrets for auth (e.g., `JWT_SECRET` / `AUTH_SECRET`).
  - Environment mode flags (`NODE_ENV`, `APP_ENV`).
