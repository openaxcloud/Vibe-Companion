# Active Context: Initial Setup & Next Steps

## Current Focus
Lay the foundation of the Memory Bank Todo app with a clean React + TypeScript structure, local state management, and localStorage persistence. No backend or advanced features yet; prioritize clarity, typing, and extensibility.

## Immediate Next Steps (Checklist)
- [ ] Bootstrap project with Vite React + TS template
- [ ] Configure TypeScript strict mode and basic ESLint/Prettier
- [ ] Define core types (`Todo`, `TodoFilter`) in `src/types`
- [ ] Implement `todoStorage` wrapper for localStorage CRUD
- [ ] Create `TodoContext` with `useReducer` for todo operations
- [ ] Build UI components: `TodoInput`, `TodoList`, `TodoItem`, filter controls
- [ ] Wire components to context and storage (load/save todos)
- [ ] Add basic styling and responsive layout
- [ ] Implement minimal tests for reducer, storage, and main components
- [ ] Document main patterns and how to add new features in `README`
