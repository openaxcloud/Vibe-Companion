Define datasource (postgresql, DATABASE_URL) and generator client. Models: User (id, email unique, passwordHash, role), Product (title, description, price, imageUrl, stock, category), Category, Order (status, amount, user, paymentIntentId), OrderItem (qty, price), CartItem (user, product, qty), timestamps.

Content generated as fallback. Please implement specific functionality as needed.
