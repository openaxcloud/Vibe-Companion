Export build function bundling src/index.tsx to public/dist/bundle.js with sourcemap in dev and minify in prod; handle CSS via loader and copy index.html to dist step (or serve from public).

Content generated as fallback. Please implement specific functionality as needed.
