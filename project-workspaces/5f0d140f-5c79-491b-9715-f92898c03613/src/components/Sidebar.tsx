import React from 'react';\nimport { useChannel } from '../contexts/ChannelContext';\nimport { useNavigate } from 'react-router-dom';\n\nconst Sidebar: React.FC = () => {\n  const { channels } = useChannel();\n  const navigate = useNavigate();\n\n  const handleChannelClick = (channelId: number) => {\n    navigate(`/chat?channelId=${channelId}`);\n  };\n\n  return (\n    <aside className='bg-gray-700 p-4 w-64'>\n      <h2 className='text-white'>Channels</h2>\n      <ul>\n        {channels.map(channel => (\n          <li key={channel.id} className='cursor-pointer hover:bg-gray-600' onClick={() => handleChannelClick(channel.id)}>{channel.name} {channel.isPrivate ? '(Private)' : '(Public)'}</li>\n        ))} \n      </ul>\n    </aside>\n  );\n};\n\nexport default Sidebar;