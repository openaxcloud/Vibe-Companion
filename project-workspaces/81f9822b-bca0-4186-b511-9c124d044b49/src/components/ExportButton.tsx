import React from \'react\';\nimport { useChat } from \'../context/ChatContext\';\nimport { useTranslation } from \'react-i18next\';\nimport { Download } from \'lucide-react\';\nimport { exportChatToPdf } from \'../utils/pdfExport\';\n\nconst ExportButton: React.FC = () => {\n  const { t } = useTranslation();\n  const { currentConversation } = useChat();\n\n  const handleExport = () => {\n    if (currentConversation) {\n      exportChatToPdf(currentConversation);\n    }\n  };\n\n  return (\n    <button\n      onClick={handleExport}\n      disabled={!currentConversation || currentConversation.messages.length === 0}\n      className={`px-4 py-2 rounded-lg text-white font-semibold transition-colors duration-200 flex items-center gap-2\n        ${!currentConversation || currentConversation.messages.length === 0\n          ? \'bg-slate-700 opacity-50 cursor-not-allowed\'\n          : \'bg-accent-600 hover:bg-accent-700 shadow-lg\'}\`}\n    >\n      <Download size={18} />\n      {t(\'chat.export_pdf\')}\n    </button>\n  );\n};\n\nexport default ExportButton;