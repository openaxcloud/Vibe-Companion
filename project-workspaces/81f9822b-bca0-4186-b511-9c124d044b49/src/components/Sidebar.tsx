import React, { useState } from \'react\';\nimport { useChat } from \'../context/ChatContext\';\nimport { useTranslation } from \'react-i18next\';\nimport { History, Plus, FileText, Code, LogOut, Search, Trash2 } from \'lucide-react\';\n\ninterface SidebarProps {}\n\nconst Sidebar: React.FC<SidebarProps> = () => {\n  const { t } = useTranslation();\n  const { conversations, startNewConversation, selectConversation, currentConversation, deleteConversation } = useChat();\n  const [searchTerm, setSearchTerm] = useState<string>(\'\');\n  const [activeTab, setActiveTab] = useState<\'chat\' | \'docs\' | \'code\' | \'history\'>(\'chat\');\n\n  const filteredConversations = conversations.filter(conv =>\n    conv.title.toLowerCase().includes(searchTerm.toLowerCase())\n  );\n\n  return (\n    <aside className=\"w-72 bg-slate-900 border-r border-slate-700 flex flex-col p-6 shadow-xl\">\n      <div className=\"flex items-center justify-between mb-8\">\n        <h2 className=\"text-2xl font-bold text-slate-50\">AI Chatbot</h2>\n        <button\n          onClick={startNewConversation}\n          className=\"p-2 rounded-full bg-primary-600 text-white hover:bg-primary-700 transition-colors duration-200 shadow-md\"\n          title={t(\'sidebar.new_chat\')}\n        >\n          <Plus size={20} />\n        </button>\n      </div>\n\n      <nav className=\"space-y-2 mb-8\">\n        <button\n          onClick={() => {\n            setActiveTab(\'chat\');\n            startNewConversation();\n          }}\n          className={`w-full flex items-center gap-3 p-3 rounded-lg transition-all duration-200\n            ${activeTab === \'chat\' ? \'bg-primary-700 text-white shadow-lg\' : \'text-slate-300 hover:bg-slate-800\'}\`}\n        >\n          <Plus size={20} />\n          <span>{t(\'sidebar.new_chat\')}</span>\n        </button>\n        <button\n          onClick={() => setActiveTab(\'docs\')}\n          className={`w-full flex items-center gap-3 p-3 rounded-lg transition-all duration-200\n            ${activeTab === \'docs\' ? \'bg-primary-700 text-white shadow-lg\' : \'text-slate-300 hover:bg-slate-800\'}\`}\n        >\n          <FileText size={20} />\n          <span>{t(\'sidebar.document_upload\')}</span>\n        </button>\n        <button\n          onClick={() => setActiveTab(\'code\')}\n          className={`w-full flex items-center gap-3 p-3 rounded-lg transition-all duration-200\n            ${activeTab === \'code\' ? \'bg-primary-700 text-white shadow-lg\' : \'text-slate-300 hover:bg-slate-800\'}\`}\n        >\n          <Code size={20} />\n          <span>{t(\'sidebar.code_sandbox\')}</span>\n        </button>\n        <button\n          onClick={() => setActiveTab(\'history\')}\n          className={`w-full flex items-center gap-3 p-3 rounded-lg transition-all duration-200\n            ${activeTab === \'history\' ? \'bg-primary-700 text-white shadow-lg\' : \'text-slate-300 hover:bg-slate-800\'}\`}\n        >\n          <History size={20} />\n          <span>{t(\'sidebar.conversation_history\')}</span>\n        </button>\n      </nav>\n\n      {activeTab === \'history\' && (\n        <div className=\"flex-1 flex flex-col\">\n          <div className=\"relative mb-4\">\n            <input\n              type=\"text\"\n              placeholder={t(\'chat.search_history\')}\n              className=\"w-full p-2 pl-10 rounded-lg bg-slate-800 border border-slate-700 text-slate-100 placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-primary-500\"\n              value={searchTerm}\n              onChange={(e) => setSearchTerm(e.target.value)}\n            />\n            <Search size={18} className=\"absolute left-3 top-1/2 -translate-y-1/2 text-slate-400\" />\n          </div>\n          <div className=\"flex-1 overflow-y-auto space-y-2 pr-2 custom-scrollbar\">\n            {filteredConversations.length > 0 ? (\n              filteredConversations.map((conv) => (\n                <div\n                  key={conv.id}\n                  className={`flex items-center justify-between p-3 rounded-lg cursor-pointer transition-colors duration-200\n                    ${currentConversation?.id === conv.id ? \'bg-primary-800 text-white\' : \'text-slate-300 hover:bg-slate-800\'}\`}\n                  onClick={() => selectConversation(conv.id)}\n                >\n                  <span className=\"text-sm font-medium truncate flex-1\">{conv.title}</span>\n                  <button\n                    onClick={(e) => {\n                      e.stopPropagation();\n                      deleteConversation(conv.id);\n                    }}\n                    className=\"ml-2 p-1 rounded-full text-slate-400 hover:text-red-500 hover:bg-slate-700 transition-colors duration-200\"\n                    title={t(\'chat.delete_conversation\')}\n                  >\n                    <Trash2 size={16} />\n                  </button>\n                </div>\n              ))\n            ) : (\n              <p className=\"text-slate-400 text-center text-sm\">{t(\'chat.no_results\')}</p>\n            )}\n          </div>\n        </div>\n      )}\n\n      {/* Main content for active tabs */}\n      {activeTab === \'chat\' && (\n        <div className=\"flex-1 flex flex-col justify-between\">\n          <div className=\"flex-1\"></div> {/* Placeholder to push content down */}\n        </div>\n      )}\n      {activeTab === \'docs\' && <DocumentUpload />}\n      {activeTab === \'code\' && <CodeSandbox />}\n\n      <div className=\"mt-auto pt-6 border-t border-slate-700\">\n        <button\n          className=\"w-full flex items-center gap-3 p-3 rounded-lg text-slate-300 hover:bg-slate-800 transition-colors duration-200\"\n        >\n          <LogOut size={20} />\n          <span>{t(\'sidebar.settings\')}</span>\n        </button>\n      </div>\n    </aside>\n  );\n};\n\nexport default Sidebar;