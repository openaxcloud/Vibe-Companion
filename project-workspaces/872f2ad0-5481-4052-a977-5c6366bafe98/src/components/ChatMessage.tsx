import React from \'react\';\nimport ReactMarkdown from \'react-markdown\';\nimport remarkGfm from \'remark-gfm\';\nimport rehypeRaw from \'rehype-raw\';\nimport { Message } from \'../lib/api\';\n\ninterface ChatMessageProps {\n  message: Message;\n}\n\nconst ChatMessage: React.FC<ChatMessageProps> = ({ message }) => {\n  const isUser = message.role === \'user\';\n\n  return (\n    <div\n      className={`flex mb-4 ${isUser ? \'justify-end\' : \'justify-start\'}\`}\n    >\n      <div\n        className={`max-w-[70%] p-4 rounded-xl shadow-lg relative \\\n          ${isUser\n            ? \'bg-indigo-700 text-white rounded-br-none glass-morphism-user\'\n            : \'bg-slate-800 text-slate-100 rounded-bl-none glass-morphism-ai\'\n          }\n        `}\n      >\n        {!isUser && (\n          <div className=\"absolute -left-10 top-0 w-8 h-8 rounded-full bg-indigo-500 flex items-center justify-center text-white font-bold text-sm\">\n            AI\n          </div>\n        )}\n        {isUser && (\n          <div className=\"absolute -right-10 top-0 w-8 h-8 rounded-full bg-slate-600 flex items-center justify-center text-white font-bold text-sm\">\n            You\n          </div>\n        )}\n        <ReactMarkdown\n          className=\"prose prose-invert prose-indigo max-w-none\"\n          remarkPlugins={[remarkGfm]}\n          rehypePlugins={[rehypeRaw]}\n        >\n          {message.content}\n        </ReactMarkdown>\n      </div>\n    </div>\n  );\n};\n\nexport default ChatMessage;\n